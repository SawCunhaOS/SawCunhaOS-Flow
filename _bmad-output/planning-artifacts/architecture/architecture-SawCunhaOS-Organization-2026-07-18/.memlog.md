---
scope: Etapa 1 (P0) do PRD SawCunhaOS Motor Fundacional Fase 1 - Fundacao de Identidade e Organizacao (Company/Department/Position/Employee/Login/Profile + sistema de aprovacao FR-23-28), Governanca de Turno (Zero Trust), Kill Switch
purpose: build-substrate + companion deck para publico nao-tecnico
altitude: initiative
updated: 2026-07-18T15:59
---

- (direction) Input: prd.md (status final) + seu memlog, escopo Etapa 1 (P0) apenas. Persistent fact: project-context.md (brownfield, DDD tactical + layered: domain/usecase/api/infrastructure/boot, specification+Bean, delegate pattern, ja documentado, ratificado como paradigma existente, nao re-derivado).
- (decision) Kill Switch (FR-13): mecanismo = Redis denylist de jti/loginId (TTL = validade restante do token), checado no filtro de autenticacao existente. Chamada a Keycloak Admin API em paralelo so pra matar sessao la tambem (defesa em profundidade), NAO e o enforcement. Rejeitado: introspeccao Keycloak per-request (mudaria modelo stateless-JWT existente).
- (decision) Bloqueio de turno (FR-9): novo Servlet Filter registrado na SecurityFilterChain APOS o filtro de autenticacao JWT (ja resolvido o usuario) e ANTES do controller/delegate. Mesmo padrao de Filter ja usado (ExceptionHandlerFilter), so muda posicao. Rejeitado: AOP/aspecto (mecanismo novo, ordem relativa a seguranca menos garantida).
- (decision) Aprovacao de Login (FR-23-28): nova entidade LoginApprovalRequest (referencia Login, guarda solicitante, nivel atual da cadeia Supervisor/Gerente/APPROVE_SYSTEM_ACCESS, prazos de SLA, escalonamento). StatusLogin ganha PENDING_APPROVAL/REJECTED so pra visibilidade grosseira. Rejeitado: tudo no StatusLogin/LoginStatusHistory existente (sobrecarregaria com estado que nao e do Login).
- (decision) Gerente do Departamento (fallback FR-23): novo campo Department.managerId (FK direta pra Employee), mesmo padrao simples de Employee.supervisor (self-FK). Rejeitado: derivar de Cargo marcado como gerencial (mecanismo novo, mais complexo).
- (constraint) flow-security-starter e biblioteca reusavel entre projetos SCOS - logica de negocio especifica (checagem de turno contra WorkSchedule de Employee) NAO pode morar la. Filter de turno vive nos modulos flow-organization-* (infrastructure/boot), registrado na chain que flow-security-starter expoe como extensivel.
- (decision) APPROVE_SYSTEM_ACCESS: NAO e mecanismo novo - reusa modelo existente Login->Profile->Resource. E um Resource/permissao normal; RH cria um Profile dedicado contendo esse Resource e atribui (principal ou adicional) diretamente aos Logins escolhidos - sem derivar de Departamento/Cargo, so RH escolhendo manualmente quem recebe aquele Profile. Bootstrap dos primeiros membros = seed Liquibase (Profile + atribuicao), mesmo padrao ja usado pra seed_data.sql.
- (event) Spine distilada: 6 ADs (Kill Switch Redis+Keycloak, Filter de turno pos-auth, Filter no app nao no starter, LoginApprovalRequest entidade propria, Department.managerId, APPROVE_SYSTEM_ACCESS via Profile existente), paradigma DDD tatico ratificado, stack seed, structural seed, capability map, deferred (Etapa 2/3).
- (event) Reviewer gate concluido (4 subagentes: reconciliacao, rubric, verificacao de versao, adversarial). Todos os achados corrigidos na spine: AD-1 ganhou ordem de escrita Redis sincrona + fail-closed explicito na leitura; AD-2/3 ganhou ancora dupla explicita (addFilterAfter JWT, addFilterBefore jDempotent); AD-4 ganhou sincronizacao mesma-transacao Login.status/LoginApprovalRequest, campo isExceptionSelfApproval, campo escalationPolicy (INDEFINITE vs AUTO_CANCEL) com estado Cancelado distinto pra FR-28, e regra de nivel inicial em t=0; AD-6 ganhou pre-requisito explicito (corrigir bug vw_login_context antes, e usar perfil PRINCIPAL nao adicional); corrigido nome do enum StatusLogin->LoginStatus (erro factual achado pela verificacao); binds estendido pra incluir FR-14; Capability Map ganhou linhas pra FR-1/2/4/8/11 (sem AD nova, cobertos por convencao ja ratificada); Design Paradigm corrigido (2 composition roots: boot REST + grpc-boot, nao 1 so); Deferred ganhou entrada explicita da defasagem de 30min (FR-8). Lint limpo (0 findings).
- (event) Deck HTML nao-tecnico publicado (Artifact) explicando Etapa 1 pra publico nao-tecnico: problema, 3 pilares, cadeia de aprovacao, seguranca por padrao, roadmap Etapa 1-3, reuso de infra. URL: https://claude.ai/code/artifact/3c7cfd55-5d32-42e7-b5a8-3057559272db
- (event) spine finalized
- (event) Implementado schema real (Liquibase, edicao direta nas definicoes, sem migration incremental, por pedido do usuario ja que projeto nao publicado): tabela SCOS_LOGIN_APPROVAL_REQUEST nova (v1.0.0/tables/), coluna DEPARTMENT.MANAGER_ID (arquivo separado por ordem de FK - Employee so existe depois), checks.yml estendido (LoginStatus ganha PENDING_APPROVAL/REJECTED, checks novos pra LoginApprovalRequest), trigger de bloqueio DELETE (reusa fn_block_delete existente), indices (FK lookup + varredura parcial de SLA). Descoberta real: trigger trg_sync_login_status ja existente sincroniza Login.status a partir de SCOS_LOGIN_STATUS_HISTORY - AD-4 da spine corrigida pra usar esse mecanismo real em vez de inventar sincronizacao nova. YAML validado sintaticamente, ids de changeset verificados unicos por arquivo.
- (event) Diagramas criados em etc/architecture/etapa-1-diagramas.md (8 diagramas Mermaid: contexto C4, modulos/dependencias, ERD Etapa 1, sequencia aprovacao de Login, sequencia bloqueio de turno, sequencia Kill Switch, maquina de estados LoginApprovalRequest, deploy/infra). Cada diagrama com paragrafo de contexto em linguagem simples. Spine atualizada: companions aponta pro arquivo de diagramas.
- (event) Validacao pos-implementacao concluida (3 subagentes: rubric v2, reality-check schema, adversarial pos-implementacao). Achados reais corrigidos: (1) Convencao de imutabilidade corrigida - trigger so bloqueia DELETE, nunca UPDATE (LoginApprovalRequest precisa de UPDATE legitimo p/ escalonamento); (2) Design Paradigm expandido - server-fat (composition root real com ScosFlowApplication, importa organization via BOM) + modulos-esqueleto notification/geotemporal (0 classes, so pom) documentados, com open question sobre qual e o deployavel real de producao; (3) AD-7 nova - checagem de filial ativa em subarvore via CTE recursiva (CompanyServiceBean.depthOf confirmado so andar pra cima, nunca pra baixo); (4) FK renomeada pra bater convencao (FK_REQUESTED_BY_LOGIN_ID_.../FK_DECIDED_BY_LOGIN_ID_...); (5) AD-4 fortalecida - dono do recalculo de SLA_DEADLINE (job agendado, mesma transacao), path explicito de escrita STATUS (Use Case escreve LoginApprovalRequest E o INSERT em LOGIN_STATUS_HISTORY, nenhum trigger conecta as duas tabelas diretamente), CHECK correlacionando escalation_policy/requested_by_login_id; (6) diagrama de estados corrigido pra vocabulario ingles (schema real), nao mais PT-BR; (7) tabela SCOS_SHIFT_ENFORCEMENT_LOG criada (FR-11, antes so placeholder) com checks e trigger de bloqueio. 2 achados de reviewers descartados apos verificacao direta contra o codigo real (CHECK constraints - existiam, reviewer nao leu checks.yml; dependencia grpc-proto->security-starter - nao existe). YAML validado, ids unicos, lint da spine limpo.
