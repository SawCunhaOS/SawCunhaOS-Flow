# ============================================================
# Copie este arquivo para .env e ajuste os valores
# cp .env.example .env
# ============================================================

# ----------------------------------------------------------
# PostgreSQL - Banco da aplicacao
# ----------------------------------------------------------
POSTGRES_USER=scos
POSTGRES_PASSWORD=changeme
POSTGRES_DB=scos

# Usuario e senha usados pela aplicacao para conectar ao banco
# Pode ser o mesmo POSTGRES_USER ou um role dedicado
APP_DB_USER=changeme
APP_DB_PASSWORD=changeme

# ----------------------------------------------------------
# PostgreSQL - Banco do Keycloak
# ----------------------------------------------------------
KC_DB_USER=keycloak
KC_DB_PASSWORD=changeme
KC_DB_NAME=keycloak

# ----------------------------------------------------------
# Keycloak Admin
# ----------------------------------------------------------
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme

# ----------------------------------------------------------
# Redis
# ----------------------------------------------------------
REDIS_PASSWORD=changeme

# ----------------------------------------------------------
# Grafana
# ----------------------------------------------------------
GRAFANA_ADMIN_PASSWORD=changeme
